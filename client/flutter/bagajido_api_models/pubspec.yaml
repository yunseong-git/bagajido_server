name: bagajido_api_models
description: Bagajido API용 Freezed + json_serializable DTO(Flutter).
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.15
  freezed: ^2.5.8
  json_serializable: ^6.9.4
  lints: ^5.0.0

# 코드 생성(프로젝트 루트에서):
#   cd client/flutter/bagajido_api_models
#   dart pub get
#   dart run build_runner build -d
